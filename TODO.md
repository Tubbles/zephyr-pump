# To Do

## OTA bring-up

- Verify DNS resolution on hardware: after a flash, run `net dns
  <an-internet-hostname>` over the console and confirm it returns an A record
  via the router-advertised DNS server. The resolver is enabled and builds
  clean, but is not yet verified on the board. See docs/LOG.md [dns].

## User written inbox
